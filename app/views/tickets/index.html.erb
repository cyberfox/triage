<h1>Tickets for <%= @lh_project.name %>, in search bin <%= @bin.name %></h1>
<%= render :partial => 'search_results', :locals => { :result => @bin, :tickets => @lh_tickets, :project => @lh_project } %>
